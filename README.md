# Setup

Use `npm install` to install dependencies.

# Run

Create `.env` file in the project root:
```json
API_URL=https://pixabay.com/api/
API_TOKEN=your-pixabay-access-token
```

Use `npm run ios` to start the app in ios simulator.
Use `npm run android` to start the app in android simulator.
